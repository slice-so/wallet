{
  "name": "slice-wallet-workspace",
  "private": true,
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "packages/wallet-primitives",
    "packages/wallet"
  ],
  "scripts": {
    "build": "bun run --cwd packages/wallet-primitives build && bun run --cwd packages/wallet build",
    "check": "bun run --cwd packages/wallet-primitives check && bun run --cwd packages/wallet check",
    "test": "bun run build && bun run --cwd packages/wallet-primitives test && bun run --cwd packages/wallet test",
    "type-check": "bun run build && bun run --cwd packages/wallet-primitives type-check && bun run --cwd packages/wallet type-check"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "bun-types": "1.3.14",
    "@biomejs/biome": "^2.4.15",
    "@types/node": "~25.6.2",
    "rollup": "^4.60.4",
    "rollup-plugin-dts": "^6.4.1"
  }
}
